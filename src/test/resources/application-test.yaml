bistro_web_app:
  order:
    discount:
      start: "14:30"
      end: "19:30"
      amount: 0.2
  security:
    username: test
    password: userpw
spring:
  datasource:
    url: jdbc:h2:mem:testdb/public
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect